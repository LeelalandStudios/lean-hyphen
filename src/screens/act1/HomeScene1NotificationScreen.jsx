import HomeScreen from "../HomeScreen.jsx";

/** Scene 1 end — home with WhatsApp badge (group activity). */
export default function HomeScene1NotificationScreen() {
  return <HomeScreen variant="whatsapp_activity" />;
}
