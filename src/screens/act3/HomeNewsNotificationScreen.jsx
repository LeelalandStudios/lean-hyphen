import HomeScreen from "../HomeScreen.jsx";

/** Home with Teen Wire breaking notification (before opening News). */
export default function HomeNewsNotificationScreen() {
  return <HomeScreen variant="news_notification" />;
}
